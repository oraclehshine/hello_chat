# Hello Chat 服务器部署文档

## 1. 部署目标

本项目可以采用“本地构建镜像 -> 推送到 Docker Hub -> 服务器拉取并启动”的方式部署。

适用范围：

- Web 前端：随 Docker 镜像一起部署
- Spring Boot 后端：随 Docker 镜像一起部署
- PostgreSQL / Redis / Zookeeper / Kafka：由服务器直接拉取官方镜像启动
- Flutter 移动端：不作为容器部署，而是编译发布到手机；移动端只需要指向你已经绑定到服务器的域名或 IP

## 2. 关键结论

### 2.1 能不能把 docker compose 和镜像一起放到 Docker Hub

可以把**镜像**放到 Docker Hub，但 `docker-compose.yml` 本身不是 Docker Hub 资源，它应该：

- 放在 Git 仓库里
- 或单独复制到服务器

推荐做法：

1. 本地构建前后端镜像
2. 推送到 Docker Hub
3. 在服务器保存 `docker-compose.prod.yml` 和 `.env.prod`
4. 在服务器执行 `docker compose pull && docker compose up -d`

### 2.2 移动端是不是只要写服务器域名就行

是，但这里要区分开发和生产：

- 开发环境：本地 `localhost:8083`
- 生产环境：服务器域名或公网 IP

当前移动端我已经补成支持自定义协议和端口，生产发布时可以这样配置：

- 直接暴露后端 `8083` 端口：`https://your-domain.com:8083`
- WebSocket：`wss://your-domain.com:8083/ws/chat`

如果你后面再上 Nginx / Caddy 做 443 统一反向代理，也可以把端口留空，直接走标准 HTTPS。

## 3. 当前项目部署结构

当前项目的实际结构如下：

- 前端容器：Nginx 提供静态资源，并反向代理 `/api/` 和 `/ws/` 到后端
- 后端容器：Spring Boot，端口 `8083`
- PostgreSQL：主数据库
- Redis：缓存与状态
- Zookeeper + Kafka：消息队列

部署后访问关系：

- Web 用户访问：`http://your-domain`
- 移动端访问后端：`http(s)://your-domain:8083/api/v1`
- 移动端 WebSocket：`ws(s)://your-domain:8083/ws/chat`

## 4. 本地构建并推送镜像到 Docker Hub

下面假设你的 Docker Hub 用户名是 `your-dockerhub-name`，版本号是 `v1.0.0`。

### 4.1 登录 Docker Hub

```bash
docker login
```

### 4.2 构建后端镜像

在 `code` 目录执行：

```bash
docker build -t your-dockerhub-name/hello-chat-backend:v1.0.0 ./backend
```

### 4.3 构建前端镜像

```bash
docker build -t your-dockerhub-name/hello-chat-front:v1.0.0 ./front
```

说明：

- 当前前端 Dockerfile 默认把 `VITE_API_BASE_URL` 构建为 `/api/v1`
- 前端容器内的 Nginx 会自动把 `/api/` 和 `/ws/` 代理到后端容器

### 4.4 推送镜像

```bash
docker push your-dockerhub-name/hello-chat-backend:v1.0.0
docker push your-dockerhub-name/hello-chat-front:v1.0.0
```

如果你想把最新版本标记为 `latest`，可以再推一份：

```bash
docker tag your-dockerhub-name/hello-chat-backend:v1.0.0 your-dockerhub-name/hello-chat-backend:latest
docker tag your-dockerhub-name/hello-chat-front:v1.0.0 your-dockerhub-name/hello-chat-front:latest

docker push your-dockerhub-name/hello-chat-backend:latest
docker push your-dockerhub-name/hello-chat-front:latest
```

## 5. 不依赖 Docker Hub 的离线部署方案

如果服务器访问 Docker Hub 超时，可以直接把本地镜像打包成 `tar` 后传到服务器。

当前项目已经导出了这些离线镜像包：

- `hello-chat-backend-v1.0.0.tar`
- `hello-chat-front-v1.0.0.tar`
- `postgres-15-alpine.tar`
- `redis-7-alpine.tar`
- `cp-zookeeper-7.7.0.tar`
- `cp-kafka-7.7.0.tar`

### 5.1 传输到服务器

```bash
scp *.tar root@your-server-ip:/opt/hello-chat/
```

### 5.2 服务器导入镜像

```bash
cd /opt/hello-chat
docker load -i hello-chat-backend-v1.0.0.tar
docker load -i hello-chat-front-v1.0.0.tar
docker load -i postgres-15-alpine.tar
docker load -i redis-7-alpine.tar
docker load -i cp-zookeeper-7.7.0.tar
docker load -i cp-kafka-7.7.0.tar
```

### 5.3 离线部署时的 `.env.prod`

离线部署建议直接指定镜像名：

```env
BACKEND_IMAGE=hshine666/hello-chat-backend:v1.0.0
FRONT_IMAGE=hshine666/hello-chat-front:v1.0.0
POSTGRES_IMAGE=postgres:15-alpine
REDIS_IMAGE=redis:7-alpine
ZOOKEEPER_IMAGE=confluentinc/cp-zookeeper:7.7.0
KAFKA_IMAGE=confluentinc/cp-kafka:7.7.0
```

这样就算服务器无法访问外网镜像仓库，也能直接 `docker compose up -d`。

## 6. 服务器准备

服务器上建议准备：

- Linux 服务器一台
- 已绑定域名
- 已开放端口：
  - `80`
  - `8083`
  - 如果后面上 HTTPS，再开放 `443`

安装 Docker 与 Compose 插件：

```bash
sudo apt update
sudo apt install -y docker.io docker-compose-plugin
sudo systemctl enable docker
sudo systemctl start docker
```

## 7. 部署文件

本项目已经补好了这些生产部署文件：

- [docker-compose.prod.yml](E:\Profile_in_college\linux\code\docker-compose.prod.yml)
- [.env.prod.example](E:\Profile_in_college\linux\code\.env.prod.example)

你在服务器上需要准备：

```text
/opt/hello-chat/
├── docker-compose.prod.yml
└── .env.prod
```

## 8. 服务器 `.env.prod` 示例

可参考 [`.env.prod.example`](E:\Profile_in_college\linux\code\.env.prod.example)：

```env
BACKEND_IMAGE=your-dockerhub-name/hello-chat-backend:v1.0.0
FRONT_IMAGE=your-dockerhub-name/hello-chat-front:v1.0.0
POSTGRES_IMAGE=postgres:15-alpine
REDIS_IMAGE=redis:7-alpine
ZOOKEEPER_IMAGE=confluentinc/cp-zookeeper:7.7.0
KAFKA_IMAGE=confluentinc/cp-kafka:7.7.0

DB_USERNAME=postgres
DB_PASSWORD=your-strong-postgres-password
REDIS_PASSWORD=
HELLO_CHAT_AUTH_SECRET=your-long-random-secret
HELLO_CHAT_ACCESS_TOKEN_SECONDS=7200
HELLO_CHAT_ADMIN_USER_IDS=1

ALIYUN_OSS_ENDPOINT=oss-cn-hangzhou.aliyuncs.com
ALIYUN_OSS_ACCESS_KEY_ID=your-oss-key-id
ALIYUN_OSS_ACCESS_KEY_SECRET=your-oss-key-secret
ALIYUN_OSS_BUCKET_NAME=hello-chat-im
ALIYUN_OSS_PUBLIC_URL_PREFIX=https://hello-chat-im.oss-cn-hangzhou.aliyuncs.com/
```

如果采用离线部署，把 `BACKEND_IMAGE` 和 `FRONT_IMAGE` 改成服务器已导入的镜像名即可。

## 9. 服务器启动步骤

### 9.1 上传部署文件到服务器

把这两个文件传到服务器：

- `docker-compose.prod.yml`
- `.env.prod`

### 9.2 登录 Docker Hub

```bash
docker login
```

如果你走离线部署，这一步可以跳过。

### 9.3 拉取镜像

```bash
cd /opt/hello-chat
docker compose -f docker-compose.prod.yml --env-file .env.prod pull
```

### 9.4 启动服务

```bash
docker compose -f docker-compose.prod.yml --env-file .env.prod up -d
```

### 9.5 查看状态

```bash
docker compose -f docker-compose.prod.yml --env-file .env.prod ps
docker compose -f docker-compose.prod.yml --env-file .env.prod logs -f backend
docker compose -f docker-compose.prod.yml --env-file .env.prod logs -f front
```

### 9.6 健康检查

后端健康检查：

```bash
curl http://127.0.0.1:8083/actuator/health
```

前端检查：

```bash
curl http://127.0.0.1
```

## 10. 域名与访问方式

### 10.1 Web 前端

浏览器访问：

```text
http://your-domain
```

### 10.2 后端接口

移动端或第三方客户端访问：

```text
http://your-domain:8083/api/v1
```

### 10.3 WebSocket

```text
ws://your-domain:8083/ws/chat
```

### 10.4 如果你已经按旧文档部署，现在又拿到了证书

这种情况很常见，不需要推倒重来，只要把入口切给 `1Panel` 即可。

当前推荐做法：

1. 保持后端、数据库、Redis、Kafka 容器继续运行
2. 把前端容器从宿主机 `80` 改成 `3000`
3. 把后端容器改成只绑定本机回环地址 `127.0.0.1:8083`
4. 让 `1Panel` 接管 `80/443`
5. 在 `1Panel` 中创建反向代理网站：
   - 域名：`hellochat.letchats.top`
   - 反代目标：`http://127.0.0.1:3000`
6. 在 `1Panel` 网站配置里启用 HTTPS，并绑定你已经拿到的证书
7. 开启 HTTP 自动跳转 HTTPS

当前生产编排已经支持这一切，在 `.env.prod` 中建议写：

```env
BACKEND_BIND=127.0.0.1:8083:8083
FRONT_BIND_PORT=3000
```

然后在服务器执行：

```bash
docker compose -f docker-compose.prod.yml --env-file .env.prod up -d
```

前端容器内部会继续代理：

- `/api/` -> `backend:8083`
- `/ws/` -> `backend:8083`

因此 `1Panel` 只需要反代前端这一个入口。

## 11. Flutter 移动端生产编译

### 10.1 当前移动端支持的生产参数

移动端现在支持这些编译参数：

- `APP_ENV`
- `API_SCHEME`
- `API_WS_SCHEME`
- `API_HOST`
- `API_PORT`

### 10.2 如果后端直接暴露 8083

如果你的服务器就是 `your-domain:8083` 提供后端接口：

```bash
flutter build apk --release \
  --dart-define=APP_ENV=prod \
  --dart-define=API_SCHEME=http \
  --dart-define=API_WS_SCHEME=ws \
  --dart-define=API_HOST=your-domain.com \
  --dart-define=API_PORT=8083
```

如果你自己给 `8083` 也配了 TLS，则改为：

```bash
flutter build apk --release \
  --dart-define=APP_ENV=prod \
  --dart-define=API_SCHEME=https \
  --dart-define=API_WS_SCHEME=wss \
  --dart-define=API_HOST=your-domain.com \
  --dart-define=API_PORT=8083
```

### 10.3 如果后端通过 443 反向代理

这是更推荐的生产方式。此时移动端可以不带端口：

```bash
flutter build apk --release \
  --dart-define=APP_ENV=prod \
  --dart-define=API_SCHEME=https \
  --dart-define=API_WS_SCHEME=wss \
  --dart-define=API_HOST=api.your-domain.com \
  --dart-define=API_PORT=
```

### 10.4 Android App Bundle

上架场景建议：

```bash
flutter build appbundle --release \
  --dart-define=APP_ENV=prod \
  --dart-define=API_SCHEME=https \
  --dart-define=API_WS_SCHEME=wss \
  --dart-define=API_HOST=your-domain.com \
  --dart-define=API_PORT=8083
```

## 12. 前端镜像部署说明

前端已经随镜像部署，不需要在服务器单独安装 Node.js。

当前前端镜像内部：

- 第 1 阶段：Node 构建 Vue 项目
- 第 2 阶段：Nginx 提供静态文件
- 自动反向代理：
  - `/api/` -> `backend:8083`
  - `/ws/` -> `backend:8083`

因此服务器只要启动 `front` 容器，就能同时提供：

- 前端页面
- 前端访问后端的代理

## 13. Redis、PostgreSQL、Kafka 是否由服务器自己拉取

是的，这样最合适。

当前推荐方式有两种：

1. 在线部署
   - 你的业务镜像：自己构建并推送到 Docker Hub
   - 中间件镜像：服务器直接拉官方镜像
2. 离线部署
   - 业务镜像和中间件镜像都在本地打包成 tar
   - 服务器通过 `docker load` 导入后启动

这样做的优点：

- 业务镜像和中间件镜像职责分离
- 升级中间件更简单
- 服务器部署更标准

## 14. 升级发布流程

以后每次发版建议按这个流程：

1. 本地修改代码
2. 本地测试通过
3. 构建新镜像
4. 推送到 Docker Hub
5. 修改服务器 `.env.prod` 中的镜像标签
6. 服务器执行：

```bash
docker compose -f docker-compose.prod.yml --env-file .env.prod pull
docker compose -f docker-compose.prod.yml --env-file .env.prod up -d
```

## 15. 常见问题

### 15.1 Docker Hub 里能不能存 compose

不能。Docker Hub 主要存镜像，`compose` 文件需要单独保存在仓库或服务器上。

### 15.2 移动端是不是只要换成线上域名就能用

原则上是，但要注意：

- 协议是 `http` 还是 `https`
- WebSocket 是 `ws` 还是 `wss`
- 端口是不是 `8083`

所以生产编译时必须带上正确的 `dart-define`。

### 15.3 后端一定要暴露 8083 吗

不一定。

有两种模式：

1. 简单模式：直接开放 `8083`
2. 推荐模式：只开放 `80/443`，由反向代理转发到后端 `8083`

如果你现在想先快速上线，可以先用简单模式。

### 15.4 前端容器和移动端是否都能共用同一后端

可以。当前架构本来就是：

- Web 前端共用同一套 `/api/v1`
- Flutter 移动端共用同一套 `/api/v1`
- WebSocket 也共用同一后端

### 15.5 服务器完全连不上外网镜像仓库怎么办

就走离线部署：

1. 本地 `docker save`
2. 传服务器
3. 服务器 `docker load`
4. `.env.prod` 里直接写镜像名
5. `docker compose up -d`

## 16. 推荐上线顺序

建议按下面顺序来：

1. 本地构建并推送前后端镜像到 Docker Hub
2. 服务器准备 `docker-compose.prod.yml` 和 `.env.prod`
3. 服务器启动 PostgreSQL / Redis / Kafka / 后端 / 前端
4. 先用浏览器验证 Web 端
5. 再用 Flutter 带生产参数编译 APK
6. 手机上测试登录、消息、群聊、朋友圈、头像上传

## 17. 当前文档对应文件

- 部署编排文件：[docker-compose.prod.yml](E:\Profile_in_college\linux\code\docker-compose.prod.yml)
- 环境模板：[.env.prod.example](E:\Profile_in_college\linux\code\.env.prod.example)
- 主项目说明：[README.md](E:\Profile_in_college\linux\code\README.md)
- 移动端说明：[mobile/README.md](E:\Profile_in_college\linux\code\mobile\README.md)
